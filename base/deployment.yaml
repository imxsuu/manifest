apiVersion: apps/v1
kind: Deployment
metadata:
  name: sjjeon-nginx
spec:
  selector:
    matchLabels:
      run: sjjeon-nginx
  replicas: 2
  template:
    metadata:
      labels:
        run: sjjeon-nginx
    spec:
      containers:
      - name: sjjeon-nginx
        image: nginx
        ports:
        - containerPort: 80
